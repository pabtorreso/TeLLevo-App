import { Type } from "@angular/core"

export interface UserModel {

    user_id: string
    first_name: string
    last_name: string
    userType: string
    jornada: string
    phone: string
    email: string
    password: string
}